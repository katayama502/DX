# demo: 認証をローカルで模擬し、コンテンツは src/generated/content.json から読む（10月共有会のデモ／開発用）
# supabase: Supabase Auth + Postgres(RLS) を使う（本番）
VITE_APP_MODE=demo
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# scripts/build-content.ts で Supabase へ投入するときだけ使う（ブラウザには渡さない）
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
